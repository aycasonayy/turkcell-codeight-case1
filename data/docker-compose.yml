version: "3.9"
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: codeight
      POSTGRES_PASSWORD: codeight
      POSTGRES_DB: codeight
    ports:
      - "5432:5432"
    volumes:
      - ./data/schema.sql:/docker-entrypoint-initdb.d/01_schema.sql
